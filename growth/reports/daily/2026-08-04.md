# Growth daily report — 2026-08-04

## Outcome

The first Meta pilot is now defined and creatively prepared, but paid delivery
remains locked at zero spend. The evidence does not yet support activation.

## What changed

- The owner authorized preparation of one Meta Traffic campaign, one combined
  English-language ad set, and two ads with a seven-day, 1,600 TRY lifetime
  ceiling.
- Google Ads and Shopify App Store Ads are excluded.
- Two creative concepts were produced from the approved MTD campaign-builder
  screenshot, each with 4:5 feed and 9:16 Story/Reels variants. One tests the
  four-step workflow and the other tests promotion mechanics. No performance,
  adoption, review, customer, or revenue claim was added.
- Exact primary text, headlines, destinations, and UTMs are recorded in the
  creative manifest.

## Meta preflight

- The MerchantCanvas business portfolio is accessible; the prior restricted
  state was not present in the inspected settings surface.
- Meta still labels the business details as unverified.
- `MerchantCanvas Global` was created in USD and UTC. Billing shows a zero
  balance, no recent spend, and no payment method.
- The MerchantCanvas Facebook Page and Dataset/Pixel were created. The
  Dataset/Pixel is connected to the ad account, and the owner has full access.
- `merchantcanvas.com` was added and verified with a production meta tag.
- No Instagram account is attached and no spend is technically possible yet.

## Measurement preflight

- Production loads `G-6L80EEB9KD` only after analytics consent and still loads
  no Meta script without an advertising grant.
- The correct GA4 property is `MerchantCanvas`; its web stream URL and
  measurement ID match production.
- A gtag command-queue mismatch was repaired and deployed in commit `09c82a6`;
  production now matches Google's documented `dataLayer.push(arguments)`
  command format.
- Google Tag Assistant found `G-6L80EEB9KD` and recorded the `Config`,
  `page_view`, and `view_product` hits with zero console errors. This verifies
  site-side transport after analytics consent.
- A subsequent MerchantCanvas property recheck passed. Realtime showed two
  active users and received `page_view`, `view_product`, and `install_intent`.
  These are controlled validation events, not campaign traffic or Shopify
  installs.
- Repository tests confirm the separate consent grants, one bounded
  `install_intent` handoff contract, required event fields, and 500 ms fail-open
  behavior.
- Cloudflare Pages production and preview now include the public Dataset/Pixel
  ID. A live essential-only check loaded neither GA4 nor Meta; advertising
  consent loaded both integrations. Meta processed PageView and ViewContent.
- The first two live outbound checks did not show `InstallIntent` in Test
  Events. The GA4 callback could end the handoff before Meta transport
  completed, so commit `4ddda52` now reserves the full bounded wait whenever
  Meta is the install transport.
- Post-deployment verification passed. One controlled product-close CTA click
  produced exactly one PII-free browser `InstallIntent`, preserved all seven
  required product, placement, destination, and UTM fields, and completed the
  redirect to the official Shopify App Store listing.
- Meta's automatic-events setup was observed off. The explicit event contract
  remains PageView, ViewContent, and InstallIntent.

## Shopify baseline

The last-30-day dashboard remains at 2 installs, 0 uninstalls, and 2 cumulative
net installs. Paid plan activations are not yet cleanly segmented into a pilot
baseline and will not be inferred from unrelated totals.

## Quality gates

- Typecheck: pass.
- Lint: pass.
- Production/static build: pass using the bundled Node runtime.
- Automated tests: 17 passed, 0 failed.
- Privacy and Terms remain drafts.

## Decision

Continue preparation only. GA4 property reporting and Meta Pixel
`InstallIntent` are verified. Do not publish or spend until billing safety,
legal approval, the custom conversion, and the owner's final paused-preview
approval are all verified.
